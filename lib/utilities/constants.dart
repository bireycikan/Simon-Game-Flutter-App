import 'package:flutter/material.dart';

const kHeightSpacer = const SizedBox(
  height: 30.0,
);

const kWidthSpacer = const SizedBox(
  width: 30.0,
);

const kAnimatedOpacityDuration = const Duration(milliseconds: 200);

const kDelayedOpacityDuration = const Duration(milliseconds: 200);

enum OpacityColor { green, red, yellow, blue }
